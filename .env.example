# SSH agent socket path (for forwarding into dev container)
# 1Password example:
SSH_AGENT_SOCK="$HOME/Library/Group Containers/2BUA8C4S2C.com.1password/t/agent.sock"
# Standard macOS SSH agent:
# SSH_AGENT_SOCK="$SSH_AUTH_SOCK"
